# OpenSpec 并行增量修复计划

## 问题概述
- 活跃的变更在归档时会执行需求级别的替换。当两个变更触及同一个需求时，第二个归档会覆盖第一个，并静默丢弃场景（例如，Windsurf 与 Kilo Code 斜杠命令更新的冲突）。
- 归档工作流（`src/core/archive.ts:191` 和 `src/core/archive.ts:501`）通过将整个需求块替换为变更增量中包含的内容来重建主规范。增量格式（`src/core/parsers/requirement-blocks.ts:113`）没有基准版本或场景级别操作的概念。
- 工具无法检测变更编写者的起始点与当前规范之间的差异，因此并行开发会在没有警告的情况下破坏事实来源。

## 观察到的故障模式
- 变更 A（`add-windsurf-workflows`）在"斜杠命令配置"下添加了一个 Windsurf 场景。
- 变更 B（`add-kilocode-workflows`）从 Windsurf 之前的规范出发，向同一需求添加了一个 Kilo Code 场景。
- 变更 A 归档后，主规范包含两个场景。
- 当变更 B 归档时，`buildUpdatedSpec` 看到一个 `MODIFIED` 块用于"斜杠命令配置"，并将该需求替换为该变更中提供的四个场景的变体。由于该文件从未了解 Windsurf，Windsurf 场景就消失了。
- 没有警告、差异或冲突指示器——归档成功完成，事实来源规范现在遗漏了一个已交付的场景。

## 根本原因
1. **仅替换语义。** `buildUpdatedSpec` 执行需求块的哈希映射替换，无法合并或比较单个场景（`src/core/archive.ts:455`-`src/core/archive.ts:526`）。
2. **缺少基准指纹。** 变更不会持久保存它们编写时所依据的需求内容，因此归档步骤无法判断当前规范是否发生了偏离。
3. **单一层级粒度。** 增量语言只理解需求。即使我们引入场景级别解析，如果没有配套的合并策略，我们仍然会丢失同级编辑。
4. **缺乏冲突用户体验。** CLI 从不强制贡献者协调并行更新。没有与 `git merge`、`git rebase` 或冲突标记等效的功能。

## 设计目标
- 无论归档顺序如何，保留每个已批准的场景。
- 检测并阻止在规范偏离编写者基准时的投机性归档。
- 提供确定性的、可审查的冲突解决流程，反映源代码控制的最佳实践。
- 保持编写体验的易用性：增量应保持为人类可编辑的 markdown。
- 支持增量采用，以便现有仓库可以在不破坏活跃工作的情况下向前推进。

## 提议的修复：分层修复方案

### 阶段 0 – 止损（检测与护栏）
1. **在每个变更旁边持久保存需求指纹。**
   - 在搭建或验证变更时，捕获每个 `MODIFIED`/`REMOVED`/`RENAMED` 条目的当前需求体，并写入 `changes/<id>/meta.json`。
   - 存储基准需求内容的稳定哈希（例如 SHA-256）和原始文本本身，以便后续合并。
2. **在归档期间验证指纹。**
   - 在 `buildUpdatedSpec` 修改规范之前，从当前规范重新计算需求哈希。
   - 如果哈希与存储的基准不同，则中止并指示用户进行变基。这使得破坏性路径变得不可能。
3. **在 CLI 输出中显示意图。**
   - 显示哪些需求已过时、何时发生偏离以及哪个变更最后修改了它们。
4. **记录临时手动缓解措施。**
   - 更新 `openspec/AGENTS.md` 和文档，以便贡献者知道在有其他变更落地时重新运行 `openspec change sync`（参见阶段 1）。

_结果：_ 我们在开发更丰富的合并方案时立即防止数据丢失。

### 阶段 1 – 添加变基工作流（编写方合并）
1. **引入 `openspec change sync <id>`（或 `rebase`）。**
   - 读取存储的基准快照、当前规范和编写者的增量。
   - 对每个需求执行三方合并。初始阶段，在 markdown 行上进行简单的 diff3 即可接受，因为我们已经在需求大小的块上操作。
   - 如果合并是干净的，用合并后的文本重写 `MODIFIED` 块，并刷新存储的指纹。
   - 如果发生冲突，在变更增量内写入冲突标记（类似于 Git），并要求编写者在重新运行验证前手动编辑。
2. **丰富验证器消息。**
   - `openspec validate` 应标记未解决的冲突标记或指纹不匹配，以便错误在工作流早期出现。
3. **可选：** 提供一个 `--rewrite-scenarios` 助手，合并场景的项目符号列表，以减少手动编辑的噪音。

_结果：_ 贡献者可以在归档之前安全地将自己的工作与最新规范协调一致，恢复真正的并行开发。

### 阶段 2 – 增加增量粒度
1. **扩展增量语言，添加场景级别指令。**
   - 允许 `## MODIFIED Requirements` 下嵌套 `## ADDED Scenarios` / `## MODIFIED Scenarios` 部分，位于需求标题下方。
   - 由存储在 `meta.json` 中的稳定场景标识符（显式 ID 或生成的哈希）支持。这让系统能够推理单个场景。
2. **教导解析器理解嵌套操作。**
   - 更新 `parseDeltaSpec` 以输出场景级别的操作，而不仅仅是需求块。
   - 更新 `buildUpdatedSpec`（或其替代品）以合并场景列表，保留顺序同时以确定性方式插入新条目。
3. **自动化迁移。**
   - 提供一个一次性命令，检查每个现有规范，注入场景 ID，并将进行中的变更增量重写为更丰富的格式。
4. **当两个变更编辑同一个场景体或描述时，继续依赖阶段 1 的变基流程解决冲突。**

_结果：_ 大多数并发更新变得可交换，大大降低了人工合并的几率。

### 阶段 3 – 结构化规范图（长期）
1. **定义稳定的需求 ID。**
   - 在规范中嵌入 `Requirement ID: <uuid>` 标记，使重命名和移动可跟踪。
   - 这可以实现未来的功能，如跨功能引用和更好的差异可视化。
2. **将规范编辑建模为 AST 上的操作。**
   - 构建需求/场景/元数据的中间表示（IR）。
   - 使用操作转换或类似 CRDT 的技术来保证合并的结合性。
3. **直接与 Git 集成。**
   - 提供可选的 `openspec branch` 脚手架，将规范变更与 Git 分支对齐，让团队利用 Git 的冲突编辑器处理 markdown IR。

_结果：_ OpenSpec 从基于替换的更新发展为弹性、保留意图的规范管理平台。

## 迁移与产品影响
- **回填元数据：** 在初始推出期间，为所有活跃变更和当前主规范添加哈希。
- **CLI 用户体验：** 新命令（`change sync`、增强版 `archive`）需要文档、帮助文本和发布说明。
- **文档和 AGENTS 更新：** 强化变基工作流，并向 AI 助手解释冲突解决方法。
- **测试：** 引入涵盖分歧需求指纹和合并解析逻辑的测试夹具。
- **遥测（可选）：** 记录指纹不匹配，以便我们了解推出后团队遇到冲突的频率。

## 未解决的问题 / 风险
- 当多个变更在不同点插入场景时，应如何排序？（考虑可选的 `position` 元数据或确定性的字母顺序回退方案。）
- 如果贡献者删除了 `meta.json` 文件，优雅的故障模式是什么？（CLI 应按需重新创建指纹。）
- 我们是否需要支持无法在归档前轻松重新运行同步命令的离线编写者？（潜在的 `--accept-outdated` 紧急逃生门。）
- 如何处理已归档的历史变更？我们可能需要一个迁移脚本，以追溯方式嵌入指纹，使重新验证能够成功。

## 立即执行的下一步行动
1. 在 `openspec change validate` 期间原型化指纹捕获，并在不匹配时阻止归档。
2. 通过基于行的 diff3 合并和冲突标记发布 `openspec change sync`。
3. 更新贡献者文档和 AI 指令，强制要求在归档前运行 `sync`。
4. 将场景级增量扩展和迁移路径规划为后续 RFC。
