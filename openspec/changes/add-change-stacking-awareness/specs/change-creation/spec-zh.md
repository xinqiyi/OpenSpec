## 新增需求

### 需求：栈元数据脚手架
变更创建工作流应支持新建或拆分变更的可选依赖元数据。

#### 场景：创建带栈元数据的变更
- **当** 使用栈元数据输入创建变更时
- **则** 创建过程应将元数据字段持久化到变更配置中
- **并且** 持久化的元数据应通过变更元数据模式规则进行验证

#### 场景：拆分生成的子变更元数据
- **当** 从拆分工作流生成子变更时
- **则** 每个子变更应包含指向源变更的 `parent` 链接
- **并且** 应包含确定性排序所需的依赖元数据
