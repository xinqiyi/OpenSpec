## 新增需求

### 需求：堆叠感知变更 planning 约定
OpenSpec 约定应为并发变更的排序和分解定义可选的元数据字段。

#### 场景：声明变更依赖关系
- **WHEN** 作者需要排序相关变更时
- **THEN** 约定应定义如何声明依赖关系以及提供/所需的能力标记
- **AND** 验证指导应区分硬性阻塞因素和软性重叠警告

#### 场景：迁移期间的依赖权威来源
- **WHEN** 堆叠元数据和 `openspec/changes/IMPLEMENTATION_ORDER.md` 同时存在时
- **THEN** 约定应将每个变更的堆叠元数据视为 spec 性依赖来源
- **AND** `IMPLEMENTATION_ORDER.md` 应被视为可选的叙述性指导

#### 场景：能力标记仍需显式排序
- **WHEN** 作者使用 `provides` 和 `requires` 标记描述能力契约时
- **THEN** 约定应要求使用显式 `dependsOn` 边来表示排序关系
- **AND** 约定应禁止将 `requires` 视为隐式依赖边

#### 场景：通过 touches 声明建议性重叠
- **WHEN** 变更可能影响与其他并发变更共享的能力/spec 领域，但不需要排序时
- **THEN** 约定应允许作者使用建议性的领域标识符（例如能力 ID、spec 领域名称或路径）声明 `touches`
- **AND** 工具应将 `touches` 视为仅提供信息（无隐式依赖边，非阻塞验证信号）

#### 场景：声明父子拆分结构
- **WHEN** 大型变更被分解为较小的片段时
- **THEN** 约定应定义父子元数据和预期的排序语义
- **AND** 文档应描述何时拆分与何时保持单个变更
