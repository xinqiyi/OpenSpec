## 1. 元数据模型

- [ ] 1.1 在变更元数据模式中添加可选的堆叠元数据字段（`dependsOn`、`provides`、`requires`、`touches`、`parent`）
- [ ] 1.2 保持元数据向后兼容，不含新字段的现有变更不受影响
- [ ] 1.3 为有效/无效元数据和模式演化行为添加测试

## 2. 堆叠感知验证

- [ ] 2.1 检测依赖循环并以确定性错误使验证失败
- [ ] 2.2 检测缺少的 `dependsOn` 目标（引用的变更 ID 不存在）并检测因未解决/循环依赖路径而被传递阻塞的变更
- [ ] 2.3 为触及相同能力/规范领域的活动变更添加重叠警告
- [ ] 2.4 当活动历史中不存在提供者时，为不匹配的 `requires` 标记发出建议性警告
- [ ] 2.5 为循环、缺少依赖、重叠警告和不匹配 `requires` 情况添加测试

## 3. 排序命令

- [ ] 3.1 添加 `openspec change graph` 以显示活动变更的依赖顺序
- [ ] 3.2 添加 `openspec change next` 以推荐顺序建议无阻塞变更
- [ ] 3.3 为拓扑排序和确定性决胜规则（同深度时按变更 ID 字典序）添加测试

## 4. 拆分脚手架

- [ ] 4.1 添加 `openspec change split <change-id>` 以搭建子片段脚手架
- [ ] 4.2 确保生成的子变更包含父/依赖元数据和存根提案/任务文件
- [ ] 4.3 在拆分过程中将源变更转换为父规划容器（不包含重复的子实施任务）
- [ ] 4.4 为拆分输出结构、源变更父转换以及在未请求覆盖模式时确定性的重新拆分错误行为添加测试
- [ ] 4.5 为 `openspec change split` 实现并测试显式覆盖模式（`--overwrite` / `--force`），用于受控的重新拆分

## 5. 文档

- [ ] 5.1 在 `docs/concepts.md` 中记录堆叠元数据和排序工作流
- [ ] 5.2 在 `docs/cli.md` 中记录新的变更命令和使用示例
- [ ] 5.3 添加将大型变更拆分为可独立合并片段的指导
- [ ] 5.4 为 `openspec/changes/IMPLEMENTATION_ORDER.md` 记录迁移指导，将其作为可选的叙述性说明，而非依赖的权威来源

## 6. 验证

- [ ] 6.1 运行针对性的变更解析、验证和 CLI 命令测试
- [ ] 6.2 运行完整测试套件（`pnpm test`）并解决回归问题
