## 新增需求

### 需求：Init 安装作用域选择
init 命令应为生成的制品支持安装作用域选择。

#### 场景：作用域默认为全局
- **WHEN** 用户运行 `openspec init` 且未显式指定作用域
- **THEN** init 应使用全局配置的安装作用域
- **AND** 如果未设置，应解析为迁移感知默认值（新创建配置为 `global`，旧版 schema 演化配置为 `project`）

#### 场景：通过标志覆盖作用域
- **WHEN** 用户运行 `openspec init --scope project`
- **THEN** init 应为该次运行使用 `project` 作为首选作用域
- **AND** 除非用户显式更改配置，否则不应修改持久化的全局配置

### 需求：Init 使用有效作用域解析
init 命令应在生成文件之前为每个工具表面解析有效作用域。

#### 场景：带回退的有效作用域
- **WHEN** 所选工具/表面不支持首选作用域
- **AND** 支持备选作用域
- **THEN** init 应在备选有效作用域生成文件
- **AND** 应在摘要中显示回退说明

#### 场景：不支持的作用域选择
- **WHEN** 所选工具/表面既不支持首选也不支持备选作用域
- **THEN** init 应在写入文件之前失败
- **AND** 应提供清晰的错误指导
