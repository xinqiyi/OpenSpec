## 1. 全局配置 + 验证

- [ ] 1.1 向 `GlobalConfig` 添加 `installScope`（`global` | `project`），新创建的配置显式默认值为 `global`
- [ ] 1.2 更新配置架构验证和已知键检查以包含安装范围
- [ ] 1.3 添加架构演进测试，确保旧配置中缺失的 `installScope` 在显式迁移之前解析为有效的 `project`
- [ ] 1.4 扩展 `openspec config list` 输出以显示安装范围及其来源（`explicit`、`new-default`、`legacy-default`）

## 2. 工具能力元数据 + 解析器

- [ ] 2.1 扩展 `AI_TOOLS` 元数据以声明每个界面（skills/commands）的范围支持
- [ ] 2.2 为 skill 和命令添加共享的安装目标解析器，使用请求的范围 + 工具支持
- [ ] 2.3 实现首选范围不受支持时的确定性回退/错误行为，包括范围支持元数据缺失时的默认行为
- [ ] 2.4 为范围解析添加单元测试（首选、回退和硬失败路径）

## 3. 命令生成合约

- [ ] 3.1 更新 `ToolCommandAdapter` 路径合约以接受安装上下文
- [ ] 3.2 更新 `generateCommand`/`generateCommands` 以通过适配器传递上下文
- [ ] 3.3 将所有命令适配器迁移到新的路径合约
- [ ] 3.4 更新适配器测试以覆盖作用域路径行为（包括 Codex 全局路径语义）

## 4. Init 命令范围支持

- [ ] 4.1 向 `openspec init` 添加范围覆盖标志（`--scope global|project`）
- [ ] 4.2 在写入构件之前，解析每个工具/界面的有效范围
- [ ] 4.3 为 skill 和命令应用能感知范围的生成/移除 planning
- [ ] 4.4 在 init 摘要输出中显示有效范围决策和回退说明
- [ ] 4.5 为全局默认、项目覆盖以及回退/错误场景添加 init 测试

## 5. Update 命令范围支持

- [ ] 5.1 向 `openspec update` 添加范围覆盖标志（`--scope global|project`）
- [ ] 5.2 使已配置工具检测和漂移检查能感知范围
- [ ] 5.3 持久化和读取每个工具/界面上次成功的有效范围，用于确定性的范围漂移检测
- [ ] 5.4 应用能感知范围的同步/移除，具有一致的回退/错误行为
- [ ] 5.5 确保范围更改更新新目标中的托管文件，并安全清理旧的托管目标
- [ ] 5.6 为全局/项目/回退/错误和重复运行的幂等性添加更新测试

## 6. 配置 UX

- [ ] 6.1 扩展 `openspec config profile` 交互流程以选择安装范围
- [ ] 6.2 在使用预设快捷方式时保留安装范围，除非显式更改
- [ ] 6.3 确保非交互式配置行为保持确定性，并附有清晰的错误信息
- [ ] 6.4 添加/调整配置命令测试以覆盖安装范围流程
- [ ] 6.5 为遗留用户添加显式选择 `global` 范围的迁移 UX

## 7. 文档

- [ ] 7.1 使用范围行为和有效范围回退说明更新 `docs/supported-tools.md`
- [ ] 7.2 使用 init/update 范围选项的示例更新 `docs/cli.md`
- [ ] 7.3 记录全局安装的跨项目影响
- [ ] 7.4 添加现有用户迁移指南，涵盖遗留默认行为和显式选择 `installScope: global`

## 8. 验证

- [ ] 8.1 运行配置、适配器、init 和 update 的定向测试
- [ ] 8.2 运行完整测试套件（`pnpm test`）并解决回归问题
- [ ] 8.3 手动冒烟测试：使用 `installScope=global` 的 init/update
- [ ] 8.4 手动冒烟测试：使用 `--scope project` 的 init/update
- [ ] 8.5 验证 Windows CI 上的路径解析行为（或使用模拟 Windows 路径的跨平台单元测试）
- [ ] 8.6 验证混合工具的跨范围 x 交付 x 命令界面能力的组合行为矩阵
