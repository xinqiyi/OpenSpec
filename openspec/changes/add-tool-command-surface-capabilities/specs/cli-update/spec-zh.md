## 新增需求

### 需求：按命令表面能力进行交付同步
update 命令应使用每个已配置工具的命令表面能力来同步制品。

#### 场景：为适配器支持的已配置工具进行命令交付
- **当** 用户运行 `openspec update`
- **并且** 交付设置为 `commands`
- **并且** 已配置工具具有适配器支持的命令表面
- **则** 系统应为活跃工作流生成或刷新命令文件
- **并且** 系统应移除该工具的受管技能目录

#### 场景：为可调用技能类型的已配置工具进行命令交付
- **当** 用户运行 `openspec update`
- **并且** 交付设置为 `commands`
- **并且** 已配置工具具有 `skills-invocable` 命令表面能力
- **则** 系统应为活跃工作流生成或刷新受管技能目录
- **并且** 系统不应将那些受管技能目录作为仅命令清理的一部分移除
- **并且** 系统不应要求为该工具生成适配器命令文件

#### 场景：命令交付但命令表面不受支持
- **当** 用户运行 `openspec update`
- **并且** 交付设置为 `commands`
- **并且** 已配置工具没有命令表面能力
- **则** 系统应在应用部分更新前以退出码 1 失败
- **并且** 输出应标识不兼容的工具和推荐的补救措施

### 需求：可调用技能类型命令表面的已配置工具检测
update 命令应将具有可调用技能类型命令表面的工具视为已配置工具（当受管技能制品存在时），包括在命令交付模式下。

#### 场景：命令交付下的可调用技能类型工具
- **当** 用户运行 `openspec update`
- **并且** 交付设置为 `commands`
- **并且** 工具没有适配器生成的命令文件
- **并且** 该工具标记为 `skills-invocable` 且已安装受管技能
- **则** 系统应将该工具包含在已配置工具检测中
- **并且** 系统应对该工具应用正常的版本/配置文件/交付同步

### 需求：更新总结反映每个工具的有效交付
update 命令应在交付意图和制品类型因工具能力不同时报告有效的制品行为。

#### 场景：命令交付中的可调用技能类型工具总结
- **当** 更新成功完成
- **并且** 交付为 `commands`
- **并且** 至少有一个更新的工具是 `skills-invocable`
- **则** 输出应包含一条清晰的说明，指出这些工具使用技能作为其命令表面
- **并且** 输出应避免暗示命令生成因错误而被跳过
