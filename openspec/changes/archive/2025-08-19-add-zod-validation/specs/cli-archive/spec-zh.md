## 新增需求

### 需求：归档验证

archive 命令应在应用变更之前进行验证，以确保数据完整性。

#### 场景：归档前验证

- **当** 执行 `openspec archive change-name`
- **则** 首先验证变更结构
- **并且** 仅在验证通过时继续执行
- **并且** 如果验证失败则显示验证错误

#### 场景：跳过验证强制归档

- **当** 执行 `openspec archive change-name --no-validate`
- **则** 跳过验证（不安全模式）
- **并且** 显示关于跳过验证的警告
