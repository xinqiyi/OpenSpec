# 实施任务

## 1. 扩展初始化守卫
- [x] 1.1 在 `openspec init` 开始时检测现有 OpenSpec 结构，进入扩展模式而不是失败。
- [x] 1.2 记录核心脚手架将被跳过，同时仍然保护缺少写入权限的情况。

## 2. 更新 AI 工具选择
- [x] 2.1 即使在扩展模式中也呈现 AI 工具选择，指示哪些工具已经配置。
- [x] 2.2 确保禁用的"即将推出"工具保持不可选择。

## 3. 生成额外的工具文件
- [x] 3.1 为新选择的工具创建配置文件，同时除了标记管理部分外，保持未触及的工具不受影响。
- [x] 3.2 在退出前总结已创建、已刷新和已跳过的工具，并返回适当的退出码。

## 4. 验证
- [x] 4.1 添加测试，覆盖重新运行 `openspec init` 以添加另一个工具，以及用户拒绝添加任何内容的场景。
