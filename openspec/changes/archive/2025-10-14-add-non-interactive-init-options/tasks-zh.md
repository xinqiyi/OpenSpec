## 1. CLI 选项注册
- [x] 1.1 将多个标志设计替换为单个 `--tools <value>` 选项，支持 `all|none|a,b,c`，并保持严格的参数验证。
- [x] 1.2 从 `AI_TOOLS` 注册表动态填充 `--tools` 帮助文本。

## 2. InitCommand 修改
- [x] 2.1 在 InitCommand 构造函数中接受单个 tools 选项，并将其贯穿到现有流程中。
- [x] 2.2 更新工具选择逻辑，为 `all`、`none` 和显式列表跳过提示。
- [x] 2.3 当解析列表包含不支持的工具 ID 时，以退出码 1 和有用消息快速失败。

## 3. 规范更新
- [x] 3.1 在变更增量中捕获非交互式场景（`all`、`none`、列表、无效），不直接修改 `specs/cli-init/spec.md`。
- [x] 3.2 文档化 CLI 帮助反映由 `AI_TOOLS` 管理的可用工具 ID。

## 4. 测试
- [x] 4.1 添加解析 `--tools` 值的单元测试覆盖，包括无效条目。
- [x] 4.2 添加集成测试覆盖，确保非交互式运行生成预期的文件和退出码。
- [x] 4.3 验证在省略 `--tools` 时交互式流程保持不变。
