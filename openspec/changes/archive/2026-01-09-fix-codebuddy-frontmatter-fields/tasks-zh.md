## 1. 实施

- [x] 1.1 更新 CodeBuddy frontmatter，使用 `argument-hint` 替代 `category` 和 `tags`
- [x] 1.2 为 `proposal` 命令添加缺失的 frontmatter 字段
- [x] 1.3 确保所有三个命令（proposal、apply、archive）具有一致的 frontmatter 结构
- [x] 1.4 通过运行 `openspec init` 和 `openspec update` 测试更改
