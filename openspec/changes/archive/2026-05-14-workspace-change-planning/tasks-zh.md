## 阶段 1：工作区设置技能

用户可测试的成果：用户可以运行工作区设置，选择哪些代理获得活动配置文件的 OpenSpec 技能，并验证所选技能仅在工作区根目录生成。

- [x] 1.1 添加一个名为"安装代理技能"的交互式工作区设置步骤，询问哪些代理应在此工作区中获得 OpenSpec 技能。
- [x] 1.2 当首选打开程序支持技能时，预选该打开程序，同时允许用户选择不同或额外的代理。
- [x] 1.3 使用现有的 `--tools all|none|<ids>` 风格支持非交互式代理选择。
- [x] 1.4 使用与仓库初始化相同的受支持技能生成工具集来验证工作区设置工具 ID。
- [x] 1.5 解析活动的全局配置文件，并使用它来选择工作区设置安装哪些工作流技能。
- [x] 1.6 确保 `openspec workspace setup` 为所选代理在工作区根目录生成或刷新 OpenSpec 代理技能。
- [x] 1.7 将设置时的技能生成限定在工作区规划主目录内；在工作区设置期间不将技能或 OpenSpec 工件写入链接的仓库或文件夹。
- [x] 1.8 在此切片中将工作区设置技能生成保持为仅限技能；即使全局交付包含命令，也不生成斜杠命令或全局命令文件。
- [x] 1.9 定义设置在人类和 JSON 输出中如何报告已生成、已刷新、已跳过、已失败和仅技能交付。
- [x] 1.10 将所选的工作区技能代理和最后应用的工作流 ID 存储在仅限工作区本地的机器状态中。
- [x] 1.11 当 `--tools` 省略时，通过跳过技能安装并给出清晰指导，保持非交互式设置兼容性。
- [x] 1.12 在交互式和非交互式模式下手动运行工作区设置，并验证所选的配置文件工作流仅落地在工作区根目录。
- [x] 1.13 审查设置 UX：提示措辞、默认值、跳过路径、配置文件/交付消息、成功输出和 JSON 输出在继续前是否清晰。

## 阶段 2：工作区技能更新

用户可测试的成果：用户可以在现有工作区中更改全局配置文件、运行工作区更新，并看到工作区本地技能刷新为所选工作流，并带有清晰的人类和 JSON 输出。

- [x] 2.1 添加一个工作区更新流程，用于刷新、添加或删除现有工作区中的 OpenSpec 代理技能。
- [x] 2.2 让 `openspec workspace update` 在从工作区内部运行时解析当前工作区。
- [x] 2.3 支持命名和选定工作区更新形式，例如 `openspec workspace update platform` 和 `openspec workspace update --workspace platform`。
- [x] 2.4 支持非交互式更新形式，例如 `openspec workspace update platform --tools codex,claude`。
- [x] 2.5 仅移除不再选中的代理的已知 OpenSpec 管理工作流技能目录。
- [x] 2.6 将工作区本地工作流技能目录与当前全局配置文件选择同步。
- [x] 2.7 在此切片中将工作区更新保持为仅限技能；即使全局交付包含命令，也不生成斜杠命令或全局命令文件。
- [x] 2.8 定义更新在人类和 JSON 输出中如何报告已刷新、已添加、已移除、已跳过、已失败和仅技能交付。
- [x] 2.9 当工作区更新在没有 `--tools` 的情况下运行时使用存储的所选代理，并在传递 `--tools` 时更新该存储的选择。
- [x] 2.10 检测工作区本地技能与活动全局配置文件的差异，并报告 `openspec workspace update` 指导。
- [x] 2.11 手动运行工作区更新，涵盖刷新、添加、移除、无操作、省略 `--tools` 和配置文件变更情况，并验证链接的仓库保持不变。
- [x] 2.12 审查更新 UX：命令形式、当前工作区检测、配置文件/交付消息、差异消息、移除消息和 JSON 输出是否可理解。

## 阶段 3：配置配置文件工作区应用

用户可测试的成果：用户可以在工作区内运行 `openspec config profile`，并选择是否将变更后的全局配置文件应用到该工作区。

- [x] 3.1 检测 `openspec config profile` 何时从 OpenSpec 工作区内部运行。
- [x] 3.2 在工作区内发生实际的配置文件或交付变更后，提示立即将变更应用于当前工作区。
- [x] 3.3 确认后，为当前工作区运行 `openspec workspace update`，而不是仓库本地的 `openspec update`。
- [x] 3.4 拒绝时，报告全局配置已更改，以及 `openspec workspace update` 可以稍后应用它。
- [x] 3.5 在工作区外部保留现有的仓库本地 `openspec config profile` 应用行为。
- [x] 3.6 保持 `openspec config profile core` 为非交互式，但在工作区内部运行时打印特定于工作区的 `openspec workspace update` 指导。
- [x] 3.7 当工作区本地技能与活动全局配置文件有差异时，在工作区内无操作配置配置文件上给出警告。
- [x] 3.8 手动在工作区内部运行 `openspec config profile`，涵盖确认、拒绝、无操作、差异警告和 `core` 预设路径。
- [x] 3.9 审查配置配置文件 UX：提示措辞、项目/工作区区分、无操作行为、预设指导和后续指导是否清晰。

## 阶段 4：工作区变更创建

用户可测试的成果：用户可以从协调根目录创建工作区级别的变更，检查其工作区规划工件，并确认链接的仓库未被编辑。

- [x] 4.1 添加内置的 `workspace-planning` 模式和模板，保持正常的提案/规范/设计/任务工件结构。
- [x] 4.2 定义 workspace-planning 规范工件，支持嵌套的 `specs/**/*.md` 输出和 `specs/<区域或仓库>/<能力>/spec.md` 的说明。
- [x] 4.3 添加从工作区协调根目录创建工作区感知的变更。
- [x] 4.4 默认将工作区范围的变更创建设置为 `workspace-planning` 模式。
- [x] 4.5 将工作区级别的变更存储在工作区规划路径下，而不是在链接的仓库或文件夹下。
- [x] 4.6 在工作区变更级别一次性捕获产品目标。
- [x] 4.7 通过工作区范围的规范或任务章节，在适用时使用已注册的工作区链接名称记录或验证受影响的区域名称。
- [x] 4.8 确保创建工作区变更不会创建仓库本地 OpenSpec 工件或编辑链接的仓库。
- [x] 4.9 在工作区外部保留仓库本地变更创建行为。
- [x] 4.10 从协调根目录手动创建工作区变更，并验证生成的工件、工作区范围的规范/任务、受影响的区域和未触及的链接仓库。
- [x] 4.11 审查变更创建 UX：目标捕获、受影响区域识别、工件路径和下一步指导感觉清晰。

## 阶段 5：规划主目录和代理上下文

用户可测试的成果：用户可以运行仓库本地和工作区变更的状态和说明，并看到解析后的规划主目录、工件路径、受影响的区域、约束和下一步。

- [x] 5.1 引入一个共享的规划主目录解析器，用于识别仓库本地与工作区规划主目录。
- [x] 5.2 使用规划主目录、变更根目录、相关工件路径、受影响的区域、下一步和操作上下文来丰富 `openspec status --change <id> --json`。
- [x] 5.3 使用解析后的工件路径（用于仓库本地和工作区范围的变更）来丰富 `openspec instructions <artifact> --change <id> --json`。
- [x] 5.4 在显式的实施工作流选择受影响的区域之前，保持工作区级别的规划作为真实源。
- [x] 5.5 在状态和说明输出中保留嵌套的工作区规范路径，而不将它们扁平化为仓库本地能力路径。
- [x] 5.6 为仓库本地和工作区范围的变更手动运行状态和说明，并验证路径和操作上下文正确。
- [x] 5.7 审查规划上下文 UX：人类输出、JSON 字段名称和下一步指导对用户和代理易于理解。

## 阶段 6：工作流技能说明

用户可测试的成果：用户可以检查重新生成的工作流技能，并验证它们是路径无关的，并告诉代理使用 CLI 报告的工件路径。

- [x] 6.1 更新生成的工作流技能模板，在工件工作前运行 `openspec status --change <id> --json`，并信任返回的规划上下文。
- [x] 6.2 更新生成的工作流技能模板，在编写工件前运行 `openspec instructions <artifact> --change <id> --json`，并使用解析后的输出路径。
- [x] 6.3 审计源工作流模板中硬编码的 `openspec/changes/<name>` 假设，并用 CLI 报告的路径指导替换它们。
- [x] 6.4 在此切片中保持工件上下文命令独立，除非丰富的状态/说明在实施过程中被证明不足。
- [x] 6.5 手动重新生成或检查已安装的工作流技能，并验证它们在工作区变更中遵循 CLI 报告的工件路径。
- [x] 6.6 保护配置文件选中的工作流技能，其工作区行为尚未实现，以免它们回退到仓库本地路径或编辑链接的仓库。
- [x] 6.7 审查代理指令 UX：说明简洁、路径无关、对不受支持的工作区工作流安全，且对仓库本地和工作区规划都实用。

## 阶段 7：验证

用户可测试的成果：用户或审查者可以从一个干净的工作区运行完整的检查清单，并比较每个早期阶段的预期与实际证据。

- [x] 7.1 添加测试，验证工作区设置在工作区根目录安装技能，并保持链接的仓库不变。
- [x] 7.2 添加测试，验证工作区更新刷新、添加和仅移除管理工作区技能目录。
- [x] 7.3 添加测试，验证工作区设置/更新使用当前全局配置文件进行工作流技能选择，同时保持工作区交付仅限技能。
- [x] 7.4 添加测试，验证 `openspec config profile` 在工作区内部可以通过 `openspec workspace update` 应用变更。
- [x] 7.5 添加测试，用于存储的工作区技能代理选择、省略 `--tools` 行为和配置文件差异报告。
- [x] 7.6 添加测试，验证 `openspec update` 从工作区规划主目录重定向到 `openspec workspace update`。
- [x] 7.7 添加测试，验证不受支持的工作区工作流技能被保护，不会指示仓库本地回退编辑。
- [x] 7.8 添加测试，验证已注册的仓库在变更创建前可见。
- [x] 7.9 添加测试，验证工作区变更创建不意味着创建仓库本地工件。
- [x] 7.10 添加测试，验证 workspace-planning 模式将嵌套的 `specs/<区域或仓库>/<能力>/spec.md` 文件解析为工作区范围的规范。
- [x] 7.11 为工作区根技能路径和工作区变更路径添加跨平台路径测试。
- [x] 7.12 更新 CLI 文档、命令帮助和 shell 补全覆盖，涵盖 `workspace update`、`--tools`、配置文件行为和工作区仅技能交付。
- [x] 7.13 运行 `openspec validate workspace-change-planning --strict`。
- [x] 7.14 在标记变更为完成之前，运行完整的验收检查清单，涵盖设置、更新、配置配置文件、变更创建、规划上下文和工作流技能。
- [x] 7.15 完成对整个工作流的最终 UX 审查，并记录任何后续修复或有意的推迟。
- [x] 7.16 在实施签署前，记录每个阶段的手动命令或交互路径、预期观察和实际观察。
- [x] 7.17 如果有单独的审查者或新的代理上下文，重新运行验收和 UX 检查清单；如果没有，从干净的临时工作区重新运行并报告证据。

## 验证证据

完成证据于 2026-05-14 记录。

自动化检查：

```bash
pnpm run build
pnpm vitest run test/commands/workspace.test.ts test/commands/artifact-workflow.test.ts test/core/workspace/skills.test.ts test/core/planning-home.test.ts test/core/templates/skill-templates-parity.test.ts
node dist/cli/index.js validate workspace-change-planning --strict
git diff --check
```

干净工作区重新运行涵盖非交互式工作区设置、工作区诊断、配置配置文件更新指导、工作区更新重定向、使用 `--areas api,web` 的工作区变更创建、嵌套工作区规范的状态/说明 JSON、链接仓库清洁度和受保护的不受支持工作流技能。

观察到的结果：

- 构建、针对性测试、严格验证和空白字符检查通过。
- 工作区设置/更新仅在工作区根目录生成技能，并保持链接的仓库未触及。
- 工作区变更创建使用了模式 `workspace-planning`，报告了受影响的区域 `api` 和 `web`，保留了嵌套的 `specs/api/login/spec.md`，并在规划期间保持 `actionContext.allowedEditRoots` 为空。
- 生成的工作流技能使用了 CLI 报告的路径和工作区保护，而不是硬编码的 `openspec/changes/<name>` 路径。
- 没有新的代理重新运行；干净的临时工作区重新运行作为后备的独立验收通行证。
