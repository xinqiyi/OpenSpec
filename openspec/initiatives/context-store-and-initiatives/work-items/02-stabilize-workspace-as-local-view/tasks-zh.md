# 将 workspace 稳定为本地视图的任务

- [x] 研究当前 workspace 运行时、指导和测试覆盖率。
- [x] 在 `direction.md` 中重新锚定指导方向。
- [x] 决定生成的指导应将持久化协调引导到
 initiative，将实现 planning 引导到 repository 本地的变更。
- [x] 决定生成的指导应停止推荐将 workspace 级别的
 `changes/` 作为 planning 归宿。
- [x] 决定 `workspace update` 刷新已有 workspace 的生成指导块。
- [x] 更新 workspace planning 操作上下文，使 Beta workspace artifact 成为
 兼容性上下文，而非真相来源。
- [x] 决定推迟正常的 doctor skill 摘要，直到用户需要
 已安装 skill 清单。
- [x] 更新 `workspace update` 措辞，包含 workspace 本地指导和
 agent skill。
- [x] 定义最小的 doctor/状态改进，涵盖本地路径、未解析
 链接和已安装的 agent skill。
- [x] 确定实现切片的目标代码/测试文件。
- [x] 在发布实现之前运行针对性的 workspace 和 artifact workflow 测试切片。
- [x] 关闭仍将 workspace 框定为持久化 planning 归宿的实时文档措辞。
