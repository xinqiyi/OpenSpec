# 上下文存储首次运行和清理 UX 任务

- [x] 决定无参数 `context-store setup` 在 TTY、非 TTY 和 `--json` 调用中的确切行为。
- [x] 设计存储 id、目标路径和 Git 初始化的交互式设置提示。
- [x] 定义管理默认值、显式路径、现有 Git repository 内的路径和非空目录的目标路径安全行为。
- [x] 实现交互式设置流程，而不改变确定性的非交互式行为。
- [x] 决定清理表面是 `unregister`、`remove` 还是两者都有。
- [x] 定义"忘记本地注册"与"同时删除本地文件"的清理语义。
- [x] 实现本地注册表清理，在删除文件前需要显式确认。
- [x] 添加保持小巧的人类输出和报告确切设置和清理状态的 JSON 输出，不含 `next_commands`。
- [x] 将 Git 初始化范围限制为本地 `git init`，无自动暂存、提交、推送、远程创建或团队策略。
- [x] 添加针对设置提示、非交互式失败、路径安全、注册表清理和 JSON 输出的专注测试。
- [x] 更新 beta 文档和 agent 手册中关于首次运行设置和清理的引用。
