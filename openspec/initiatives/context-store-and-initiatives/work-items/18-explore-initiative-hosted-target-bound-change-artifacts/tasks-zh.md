# 探索倡议托管的目标绑定变更 artifact——任务

- [x] 创建第 18 项工作项跟踪笔记。
- [x] 将第 18 项从通用的变更家园配置重构为倡议托管的目标绑定变更 artifact。
- [ ] 审计命令、template、验证、archive、应用、补全和文档中关于 repository 本地 `openspec/changes/` 的假设。
- [ ] 为倡议工作项、简报、目标绑定变更、artifact 家园和可编辑目标定义面向用户的命名。
- [ ] 决定倡议托管 artifact 是否可以升级为可执行变更，以及首先需要哪些目标元数据。
- [ ] 决定 repository 本地与倡议托管 artifact 的配置或选择加入界面。
- [ ] 定义 `openspec new change` 如何选择和报告 artifact 家园、实现目标、倡议链接和操作上下文。
- [ ] 定义倡议链接和 workspace 指导如何发现 artifact 家园和目标 repository 映射。
- [ ] 决定倡议托管的目标绑定变更如何绑定到 repository spec、实现根目录、分支和本地检出路径。
- [ ] 决定倡议托管的目标绑定变更的验证、应用、archive、同步和冲突行为。
- [ ] 定义工作目标、artifact 家园、实现目标、允许的编辑根目录、不支持的声明周期命令和后续命令的 agent JSON 契约。
- [ ] 记录现有 repository 本地和 workspace 本地变更的兼容性行为。
- [ ] 生成建议、选择加入/配置示例、受影响的命令列表以及实现的通过/不通过标准。
