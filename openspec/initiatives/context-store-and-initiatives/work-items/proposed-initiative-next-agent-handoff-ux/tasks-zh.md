# 拟议倡议下一步 / Agent 交接用户体验任务

这些仅是讨论任务。在路线图位置和范围确认之前，请勿实施。

- [ ] 决定是否将其添加为编号路线图项。
- [ ] 决定该命令是 `initiative next`、workspace 倡议打开指南，还是 repository 本地状态指南。
- [ ] 决定 Agent 交接的最小 JSON 输出契约。
- [ ] 决定该命令返回一个下一步操作还是多个选项。
- [ ] 决定错误和空状态行为。
- [ ] 决定实际工作进度/状态是否明确排除在范围之外。
- [ ] 决定是否将 `use-openspec` 作为托管默认 skill 发布。
- [ ] 决定 `use-openspec` 是默认配置成员还是单独的始终在线指南 skill。
- [ ] 决定 `use-openspec` 如何与面向命令的交付交互。
- [ ] 决定上下文存储倡议、repository 本地变更和 workspace 视图的最小产物放置指南。
